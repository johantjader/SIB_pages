---
phase: 05-final-refinements
plan: 02
type: execute
wave: 1
depends_on: ["05-01"]
files_modified:
  - .github/workflows/sync-content.yml
  - docusaurus.config.ts
  - src/pages/index.tsx
  - src/components/HomepageFeatures/index.tsx
autonomous: true
requirements: [UX-02, CI-CD-01]

must_haves:
  truths:
    - "Sync workflow supports repository_dispatch for zero-touch automation"
    - "Boilerplate Docusaurus SVGs are replaced with brand-aligned placeholders"
    - "Footer links point to official SIB/Digg resources"
  artifacts:
    - path: ".github/workflows/sync-content.yml"
      provides: "Automated trigger support"
      contains: "repository_dispatch"
---

<objective>
Fully automate the content sync pipeline and perform a final UX audit to remove all remaining boilerplate elements.

Purpose: Achieves a truly seamless maintenance experience and ensures the portal is production-ready and professionally branded.
Output: An automated pipeline and a polished, white-labeled landing page.
</objective>

<execution_context>
@C:/Users/jcols/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/jcols/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-final-refinements/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize Sync Automation</name>
  <files>.github/workflows/sync-content.yml</files>
  <action>
    - Ensure `repository_dispatch: { types: [sync_content] }` is correctly configured in `.github/workflows/sync-content.yml`.
    - Verify that the workflow correctly handles the `PAT_TOKEN` for pushing back to `SIB_pages`.
  </action>
  <verify>Check `.github/workflows/sync-content.yml` triggers.</verify>
  <done>Zero-touch sync is supported.</done>
</task>

<task type="auto">
  <name>Task 2: Replace Boilerplate UX Elements</name>
  <files>src/pages/index.tsx, src/components/HomepageFeatures/index.tsx, docusaurus.config.ts</files>
  <action>
    - Replace default Undraw SVGs (`undraw_docusaurus_mountain.svg`, etc.) with neutral geometric shapes or icon-based placeholders that align with Digg.se style.
    - Update Footer links in `docusaurus.config.ts`: Remove Docusaurus community links and replace them with "Om SIB", "Kontakt", and official links if available.
    - Ensure the "Nyheter" section title and description on the homepage are polished.
  </action>
  <verify>Visual audit of the landing page and footer.</verify>
  <done>Boilerplate is fully removed.</done>
</task>

<task type="auto">
  <name>Task 3: Final Production Verification</name>
  <files>build/</files>
  <action>
    - Perform a final `npm run build`.
    - Verify all links and diagrams in the generated site.
  </action>
  <verify>Build success and visual integrity.</verify>
  <done>Portal is ready for handover.</done>
</task>

</tasks>

<verification>
Test the Repository Dispatch trigger manually (via API or UI) if possible. Perform a final sweep of the site for any remaining 'Docusaurus' text or generic assets.
</verification>

<success_criteria>
1. Content syncs automatically on external push.
2. The landing page is professional and free of template boilerplate.
3. Navigation and footer are fully customized for SIB.
</success_criteria>

<output>
After completion, create `.planning/phases/05-final-refinements/05-02-SUMMARY.md`
</output>
