---
phase: 01-foundation-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - docusaurus.config.ts
  - tsconfig.json
  - static/.nojekyll
  - src/css/custom.css
  - docs/intro.md
  - sidebars.ts
autonomous: true
requirements: [SETUP-01, SETUP-02, SETUP-04]

must_haves:
  truths:
    - "Docusaurus v3 dependencies are in package.json"
    - "docusaurus.config.ts uses TypeScript and has correct URL/baseUrl"
    - "Local build 'npm run build' completes without errors"
  artifacts:
    - path: "package.json"
      provides: "Docusaurus v3 core and preset-classic"
      contains: ""@docusaurus/core": "^3."
    - path: "docusaurus.config.ts"
      provides: "Site configuration for jcols.github.io/SIB_pages"
      contains: "baseUrl: '/SIB_pages/'"
    - path: "static/.nojekyll"
      provides: "Jekyll bypass for GitHub Pages"
  key_links:
    - from: "docusaurus.config.ts"
      to: "GitHub Pages"
      via: "url and baseUrl settings"
      pattern: "url: 'https://jcols.github.io'"
---

<objective>
Initialize the Docusaurus v3 environment with TypeScript and configure it for hosting on GitHub Pages.

Purpose: Establishes the core framework and ensures compatibility with the target hosting environment.
Output: A functional Docusaurus project ready for content migration and deployment.
</objective>

<execution_context>
@C:/Users/jcols/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/jcols/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-deployment/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap Docusaurus v3 with TypeScript</name>
  <files>package.json, docusaurus.config.ts, tsconfig.json</files>
  <action>
    Initialize a new Docusaurus v3 project in the root directory using the classic template and TypeScript.
    Command: `npx create-docusaurus@latest . classic --typescript --force`
    (Note: Using --force because .git and .planning directories already exist).
    
    After initialization, verify that Docusaurus v3 is installed by checking `package.json`.
  </action>
  <verify>`npm list @docusaurus/core` shows version 3.x</verify>
  <done>Docusaurus v3 project structure is created in the root directory.</done>
</task>

<task type="auto">
  <name>Task 2: Configure for GitHub Pages</name>
  <files>docusaurus.config.ts, static/.nojekyll</files>
  <action>
    Modify `docusaurus.config.ts` to include the specific GitHub Pages settings from the research:
    - `url: 'https://jcols.github.io'`
    - `baseUrl: '/SIB_pages/'`
    - `organizationName: 'jcols'`
    - `projectName: 'SIB_pages'`
    - `trailingSlash: true`
    
    Update the site title to "SIB Normativa Artefakter".
    
    Create an empty `.nojekyll` file in the `static/` directory to prevent GitHub Pages from ignoring files starting with underscores.
  </action>
  <verify>
    `grep "baseUrl: '/SIB_pages/'" docusaurus.config.ts` and `ls static/.nojekyll`
  </verify>
  <done>`docusaurus.config.ts` is configured and `.nojekyll` exists.</done>
</task>

<task type="auto">
  <name>Task 3: Verify Build</name>
  <files>build/</files>
  <action>
    Run the production build to ensure all configurations are valid and there are no broken links in the default template.
    Command: `npm run build`
  </action>
  <verify>The build directory exists and contains index.html</verify>
  <done>`npm run build` completes successfully without errors.</done>
</task>

</tasks>

<verification>
Ensure that the local project is ready for deployment. The 'build' directory should contain the generated static site.
</verification>

<success_criteria>
1. Docusaurus v3 is initialized with TypeScript.
2. Configuration points to the correct GitHub Pages URL.
3. Build process is functional.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-deployment/01-01-SUMMARY.md`
</output>
